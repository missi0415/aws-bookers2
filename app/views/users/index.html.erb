<div class="container">
<div class="row">
<div class='col-lg-4'>
    <h2>User info</h2>
      <table class='table'>
        <tr><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></tr>
        <tr>
          <th>name<th>
          <th><%= current_user.name %><th>
        </tr>
        <tr>
          <th>introduction<th>
          <th><%= current_user.introduction %><th>
        </tr>
      </table>
      <div>
        <%# if @user.id == current_user.id %>
        <%= link_to edit_user_path(@user) do %>
        <div class="btn btn-outline-secondary btn-block fas fa-user-cog "></div>
        <% end %>
        <%# end %>
        </a>
      </div>

      <h2 class="mt-3">New book</h2>
      <%= form_with model: @book, local:true do |f| %>
      <div class="form-group">
        <label for="book_title">Title</label>
        <p><%= f.text_field :title %></p>
      </div>
      <div class="form-group">
      <label for="book_opinion">Opinion</label>
        <p><%= f.text_area :body %></p>
      </div>
      <div class="form-group">
        <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book" />
         <% end %>
      </div>
</div>

<div class='col-lg-8'>
  <h2>Users</h2>
  <table class='table table-hover table-inverse'>
    <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
   <tr>
     <td><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></tb>
     <td><%= user.name %></td>
     <td><%= link_to "Show", user_path(user.id) %> </td>
   </tr>
    <% end %>
    </tbody>
    </table>
    </div>
  </div>
</div>