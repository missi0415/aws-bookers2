users show
<div class "container">
<div class="row">
  <div class='col-lg-4'><%= render 'users/list', users: @users %></div>
  <div class="profile-container">
  <h3 class="profile-name"><%= @user.name %></h3>
    <div class='col-lg-8'>
    <h2>Books </h2>
      <table class='table table-hover table-inverse'>
      <thead>
        <tr>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th colspan="3"></th>
        </tr>
        </thead>
        <tbody>
        <% @books.each do |book| %>
        <tr>
          <td><p><%= attachment_image_tag book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></p></tb>

          <td><%= link_to book_path(book.id) do %>
            <%= book.title %>
            <% end %>
          </td>
          <td><%= book.body %></td>
          <td>

          </td>
          </tr>
          <% end %>
    </tbody>
  </table>
</div>

<%#=@user.name %>
