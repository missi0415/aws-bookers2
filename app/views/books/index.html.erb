userinfoの画面
userinfo edit
 <p>Title Opinion</p>

<% @books.each do |book| %>
<div>
  <p>名前</p>
   <p>投稿ユーザー画像：<%= attachment_image_tag book.user, :profile_image_id, :fill, 60, 60, fallback: "no_image.jpg" %></p>
  <P> <%= link_to book_path(book.id) do %>
        <%= book.title %>
      <% end %>
  </p>
  <p><%= book.body %></p>

</div>
<% end %>

<h1>New book</h1>
<%= form_with model: @book, local:true do |f| %>
  <h4>New book</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>